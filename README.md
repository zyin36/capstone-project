Ensure you have Node.js version v22.22.0. You can confirm this doing node -v in your terminal.

If not, you probably cannot run this visualizer.

Node.js download website: https://nodejs.org/en/download

To start the web server, first run `npm install --legacy-peer-deps` in this directory, then run `npm run dev`
