# Tests

## Requires python

### Example
Run `./src/tests/generateLargeData.py`
Data is outputed to `src/tests/data/large_chart.json`

It seems like around 5-10MB json files or 70-120K quads we would want to start culling. But, until then we can just draw everything